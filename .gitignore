# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Output of go coverage
*.out

# Go workspace
go.work
go.work.sum

# Dependency directories
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Debug
debug
__debug_bin*

# Local environment
.env
.env.local
local-only/

# server.json IS tracked (see server.json.tmpl comment + CLAUDE.md) — the
# release job re-renders and commits it every release, same as CHANGELOG.md.
# Only the large build outputs below are gitignored.
/mcpb-dist/
*.mcpb
*.mcpb.sha256
